apiVersion: v1
kind: Service
metadata:
  name: ai-operator-metrics
  namespace: ai-inference-orchestrator-system
  labels:
    control-plane: controller-manager
    app.kubernetes.io/name: ai-inference-orchestrator
spec:
  selector:
    control-plane: controller-manager
  ports:
    - name: metrics
      port: 8080
      targetPort: 8080